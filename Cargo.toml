[package]
name = "rustarus"
version = "0.1.0"
edition = "2024"

[dependencies]
uefi = "0.35"
spin = { version = "0.10", default-features = false, features = ["spin_mutex"] }

[profile.release]
opt-level = "s"
lto = true
panic = "abort"

[profile.dev]
panic = "abort"
